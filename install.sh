#!/bin/sh

function basic_install() {
    # Create folders
    mkdir -p ~/.config/{nvim, qutebrowser}/
}

function full_install() {
    # Create folders
    mkdir -p ~/.config/{nvim, qutebrowser, spotify-tui}/
}

